# RBAC for the NestJS API (K8sOrchestrator) to manage the WAHA StatefulSet.
# When NestJS runs as a pod inside the cluster, it uses this ServiceAccount.
# When running outside (on the host), create a long-lived token from this SA
# and add it to KUBECONFIG on the API server.
#
# To create a static token for out-of-cluster use:
#   kubectl create token wago-api --duration=8760h
# Then add to /opt/wago/kubeconfig as a user token.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: wago-api
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: wago-api-role
  namespace: default
rules:
  - apiGroups: ["apps"]
    resources: ["statefulsets"]
    resourceNames: ["waha"]
    verbs: ["get", "patch"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: wago-api-rolebinding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: wago-api
    namespace: default
roleRef:
  kind: Role
  name: wago-api-role
  apiGroup: rbac.authorization.k8s.io
